<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercise 16</title>
    <style>
        .mid {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        body {
            overflow: hidden;
            width: 100%;
            height: 100vh;
            background: linear-gradient(rgb(60, 60, 199), rgb(108, 26, 171));
        }

        #entryButton {
            border: 1px solid black;
            outline: 1px solid black;
            outline-offset: 2px;
            cursor: pointer;
            width: 150px;
            height: 80px;
            border-radius: 1rem;
            background-color: rgb(5, 164, 189);
            font-size: 2rem;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            transition: transform 80ms ease-out;
            transition: background-color 80ms ease-in;
        }
        #entryButton:hover {
            background-color: rgb(4, 142, 163);
        }
        #entryButton:active {
            transform: scale(0.90);
        }

    </style>
</head>

<body class="mid">
    <button type="button" id="entryButton" class="mid">
        Enter
    </button>
    <script src="/index.js"></script>
</body>

</html>