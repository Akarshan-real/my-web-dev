import "./Navbar_todos.css"
const Navbar_todos = ({info}) => {
  return (
    <div className={`w-full h-fit m-0 px-[5%] py-1 whitespace-normal `}>
      {info}
    </div>
  )
}

export default Navbar_todos
